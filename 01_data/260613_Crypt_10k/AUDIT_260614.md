# Crypt_10k audit — 2026-06-14 (post rightward-only overhaul)

**Scope:** the 440 readable (under-7680-token) CoTs, full single-reviewer coverage. The 357 over-cap CoTs are
excluded (unreadable by an agent, and not training candidates). 20-agent cold audit run as a Workflow
(10 reasoning, only `question.txt`+`tree_cot.txt`; 10 format, template+CoT) per `HOW_TO_AUDIT_A_NEMOTRON_CATEGORY.md`.

## Headline
- **The trainable set (145 = GT-True ∩ under-cap) is clean** — its worst finding is a score **9** (the allowed
  stated query-only / free-digit defaults). No serious defect touches training data.
- Every serious reasoning flag (score ≤ 6) is a **GT-False `little_endian`** puzzle that ends in the correct
  `~concat` default — none are trainable.

## Scores
| axis | n | 10 | 7–9 | 4–6 | 0–3 |
|---|---|---|---|---|---|
| Reasoning | 440 | 298 (68%) | 137 (31%) | 5 (1%) | 0 |
| Format | 440 | 233 (53%) | 207 (47%) | 0 | 0 |

## Deterministic checks (whole corpus, 100%)
- **Step 5** real `rfind('}')` extractor: **0** boxes extract empty.
- **Step 5b** forbidden tokens (`≠`, ` guess`, `= unknown`, `exotic`, `a mod b`, `branch fails`, `too smart`): **0** each.

## Reasoning ≤ 6 — triaged (all GT-False little_endian, all end in ~concat)
- **FALSE POSITIVES (2):** [little_endian_08111d57](little_endian_08111d57/track/tree_cot.txt),
  [little_endian_2ff200fb](little_endian_2ff200fb/track/tree_cot.txt) — flagged "pigeonhole 9 digits / 10
  symbols is wrong, some symbols can be 0." Verified WRONG: the QUERY operands impose leading-digit constraints
  too, so **all 10 symbols are leading → all `{1-9}` → genuine pigeonhole → correctly unsolvable**. The cold
  auditor missed the query-operand leads. Reasoning is sound.
- **Low-severity §4 wrinkles (3):** [little_endian_9c697047](little_endian_9c697047/track/tree_cot.txt),
  [little_endian_a1503629](little_endian_a1503629/track/tree_cot.txt),
  [little_endian_ae4aef23](little_endian_ae4aef23/track/tree_cot.txt) — the non-monotone `~sub` single-value
  witness picks an unconvincing representative / an `f`-lock rests on an assignment a later all-different would
  reject. The puzzles are genuinely unsolvable and end in the correct `~concat`; only the *narration* of the
  binding step is thin. **Not trainable.** Open: optionally tighten the non-monotone two-sided witness (same
  family as the earlier binding-value work) — low priority since GT-False.

## Reasoning 7–9 — soft patterns (not bugs)
- **Single-value "compression"** (most of the 137): a dead branch shows one binding value (`no digit of X is
  left` after one probe) rather than every value. Sound; the binding value is the closest call.
- **ASCII unseen-digit default / smallest-free / query-only default**: stated repeatable defaults — allowed by
  the rubric, not guesses. All in GT-False puzzles.

## Format sub-10 (206) — 100% template-documentation gaps, **not CoT defects** (FIXED)
My template rewrite under-documented real solver lines. The CoTs are correct; the template was stale/incomplete.
Patched `cot_template.md`:
| flagged line | count in corpus | fix |
|---|---|---|
| terminal `… is not solvable` (template still said `cannot be solved`) | 191 | terminal-annotation refs updated |
| `X is not pinned by any example; go with the smallest` | 66 | documented (§5) |
| `~sub … leading digits differ by at most 2, |X-Y| ≤ 2` (LEADPAIR D) | 1573 | documented (§5) |
| Conclusion `… appears only in the query, so no reading can determine it …` | 117 | documented (Conclusion a') |
| `All-different: X and Y are both d … — no` | 1295 | documented (contradiction lines) |
| `all symbols pinned but the equations don't all check — no` | 5278 | documented |
| `Elimination: only k distinct digits remain for N symbols — impossible, no` | 100 | documented |
| `This reading order is illegal; skip it completely.` | 15 | documented |

## Post-fix delta re-audit (2026-06-14, after all-different + ~sub fixes shipped)
The 3 reasoning bugs were fixed (all-different inline-lock guard; ~sub `−|a−b|−2` formula + §4 enumeration),
which changed 117 puzzles' line forms. Re-checked the **delta only** (don't re-read unchanged puzzles):
- **Deterministic (100%):** 22,308 `~sub` formula lines 0 inconsistent; 0 old-style `~sub` witnesses left;
  112 enumeration blocks 0 verdict↔conclusion mismatches; 0 punctuation bugs.
- **Cold re-audit (6 agents, 117 changed-form puzzles):** Reasoning **0 below 7** (72×10, 44×9) — every 9 was
  the *intentional* rightward-only-on-`little_endian` behavior or the stated concat-default rule, not a defect;
  auditors confirmed the new `~sub` enumeration and all-different forms are sound. Format surfaced (a) `\boxed{}`
  "broken box" flags = **render false-positives** (Step-5 `rfind('}')` extracts correctly; the answers just
  contain brace cipher-symbols), and (b) a remaining **template gap** — the operator-family **lock-failure**
  lines (`no [~X] variant gives […], so [LABEL] = [~X] fails.` 22,235×; `… beyond ±2 — no.` 1,925×; ranged
  `… in [lo,hi]` lock header) — now **documented**.

## Template reconciliation — converged (4 rounds)
The format axis is a template-completeness check; each round surfaced rarer long-tail line forms, now all
documented: R1 (terminal/free-digit/LEADPAIR-D/query-only-clause/dead-ends), R2 delta (lock-FAILURE lines:
`no [~X] variant gives …, fails`, `beyond ±2 — no`, ranged `in [lo,hi]` header), R3 (post-lock cross-check
`… also appears in EX[i]; check …; Confirmed.`, all-concat bridge, concat-short-circuit bare-Conclusion
exception, LEADPAIR-D-in-§4), R4 on a **fresh** 73-puzzle sample (the STEP-2b free-family-choice string, bare
`Impossible.` — both now added). **R4 result: 68/73 perfect, 0 below 9**, remaining flags only paraphrase/
cosmetic — the auditor independently re-confirmed the concat short-circuit bare-Conclusion as a false-positive
(documented). The deliverable CoTs were unchanged through reconciliation (only the spec was completed); a
100%-coverage line-form sweep (286 distinct forms) corroborates. Deterministic delta checks: 22,308 ~sub lines
0 inconsistent, 112 enumeration blocks 0 verdict mismatches, 0 punctuation bugs, 0 forbidden tokens, 0 empty boxes.

## Verdict
Training data is clean (trainable now 141 after the −5 from ~sub enumeration). Reasoning has **no real defect**
in either pass — the new `~sub`/all-different forms are sound. Format was 100% template-doc drift (the template
under-documented the solver's full line inventory); reconciled across both passes. Render flags killed by the
real extractor. The only standing non-defect is the deliberate rightward-only-on-`little_endian` → concat design.
Single-reviewer pass — not a zero-defect proof; for higher assurance, a 3-vote pass on the `little_endian`
non-monotone class would confirm the 3 wrinkles.
