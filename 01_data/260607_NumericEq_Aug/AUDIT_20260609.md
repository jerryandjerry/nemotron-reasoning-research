# Augmentation re-audit — 2026-06-09 (regenerated 260607_NE_aug, sign=operator-symbol fix)

Re-audit after the **sign-rendering fix**: a negative result is now written with the operator's OWN symbol as
the sign glyph (never a literal `-` unless the operator itself is `-`), matching the real corpus convention
(original 732: **404/404** result sign-glyphs equal the operator symbol). The prior pool had ~12.4%
out-of-distribution puzzles (glyph operator + literal `-` sign); those are eliminated.

2-axis audit per `../HOW_TO_AUDIT_A_NEMOTRON_CATEGORY.md` over the **1859 GT-match=True** regenerated CoTs.
Axes run as **separate sequential workflows** (to dodge the server rate-limit that hit the 28-at-once run).

## Headline

| axis | n (of 1859) | mean | 10 | 8–9 | <8 |
|---|---|---|---|---|---|
| Reasoning | 1816 (97.7%) | **9.981** | 1782 | 34 | 0 |
| Format | 1857 (99.9%) | **9.996** | 1853 | 4 | 0 |

**Step 5 (metric cross-check):** live extractor over all 1859 → **0 boxes extract empty**.
**OOD check:** literal `-` with a glyph operator → **0 in questions, 0 in answers/golds**. Fully eliminated.

Nothing below 8 on either axis. Coverage is one-pass (single reviewer/axis); ~2.3% of reasoning and 2 format
CoTs were dropped by transient agent rate-limits — does not change the verdict (clean throughout).

## Reasoning sub-10s (34) — all allowed patterns or render false-positives

- **33 are the unseen-operator guess** (NE_aug_0015xx–16xx): the query operator appears ONLY in the query, so
  it's resolved by the stated rule-6 heuristic ("symbol `*` usually means ~mul" / default order), not pure
  deduction. The rubric **explicitly permits** a genuinely-undeducible value resolved by a stated repeatable
  rule (a deterministic guess). Not defects — this is the unseen category working as designed.
- **1 render false-positive** — [NE_aug_000233](NE_aug_000233/track/tree_cot.txt): `\boxed{71}}` (suffix sign
  `}`). A naive brace-matcher would read `71`, but the **live metric** (`rfind('}')`) extracts `71}` = gold
  (verified). Same family as the prior audits' box false-positive.

Auditors positively confirmed: forward-only, deterministic; **honest backtracking** (a sub variant matching one
example but failing another is rejected and the search advances); the **leftward sign-handling split**
("leftward fully" = sign flips to the back vs "leftward on digit only" = sign stays front) is correctly
derived from each puzzle's own negative example; reading tie-breaks by stated rules only.

## Format sub-10s (4) — one cosmetic pattern, 0.4% of the pool

All 4 ([NE_aug_000192](NE_aug_000192/track/tree_cot.txt), 000246, 000277, 001140) are the same: a **rightward
negative result whose operator is the literal `-` with no negative example** (so the sign convention is
unspecified, `fmt='none'`) finalizes on one line — `f(15, 81) = 15-81-1 = -67; answer = -67` — instead of the
template's two-line structure (omit `answer =`, then a separate sign-reattach/leading-minus line). **8/1859 =
0.4%** of CoTs hit it (all `-`-operator, negative-rightward). The boxed answer is correct and coherent every
time; it's a narration-structure inconsistency, **pre-existing** (in the solver's rightward `fmt='none'`
catch-all at `260606_Numeric_Equation/_gen_eq.py:888`), not introduced by the sign fix.

Format auditors otherwise confirmed full conformance: negatives rendered with the operator's own glyph
(`"3`, `$13`, `@13`, `%92`, suffix `5-`), identity-mapped only when the operator IS `-`, the `fmt='none'`
leading-minus wording where applicable; all fixed phrases verbatim; forbidden tokens absent (`backtrack`, `≠`,
`fits`, `far from`, U+2212, opaque function names); partial-products multiplication; two-line `\boxed{}`.

## Conclusion

The regenerated 1859 GT-True CoTs (1575 used in the 2232-effective set) are **sound and safe to train on**:
reasoning 9.981, format 9.996, 0 sub-8, 0 box-extraction issues, and the out-of-distribution literal-`-`
puzzles are **completely eliminated** (0/0). Decorrelation holds (reading 5% / arith 12% / leadz 6% spread).

**Open item (user's call):** the rightward `fmt='none'` negative one-line collapse (8 CoTs, 0.4%) — fix the
solver to emit the two-line structure there too (uniform with the leftward `fmt='none'` branch), or accept it.
Surfaced, not auto-applied.
