# Augmentation audit — 2026-06-07 (260607_NE_aug, the 464 GT-match=True CoTs)

20-agent, 2-axis audit per `../HOW_TO_AUDIT_A_NEMOTRON_CATEGORY.md`, over the **464 GT-match=True** augmentation
CoTs (the actual training candidates). 10 reasoning-cold (question.txt + CoT only) + 10 format (with the
synced `260606_Numeric_Equation/cot_template.md`). Balanced disjoint slices (~46 each).

## Headline

| axis | n | mean | 10 | 7–9 | <7 |
|---|---|---|---|---|---|
| Reasoning | 464 | **9.99** | 458 | 6 | 0 |
| Format | 463* | **9.95** | 439 | 24 | 0 |

\*Format ran in two passes: the first 5 slices hit a transient server rate-limit; the 5 were re-run cleanly.

**Step 5 (metric cross-check):** 0/464 boxes extract empty.

Nothing below 7 on either axis. Every sub-10 is either a transient filesystem artifact, a rule-driven
score-9, or a template-documentation gap — none is a reasoning or answer defect.

## Reasoning sub-10 (6)

| ids | score | finding | verdict |
|---|---|---|---|
| 265, 116, 132 | 8–9 | "CoT missing at `track/`, only in `track 2/`" | **Transient Synology sync-conflict.** The audit launched seconds after writing 500 files on the cloud drive; auditors hit mid-sync conflict copies. **Verified after sync settled: 0/500 missing canonical `track/tree_cot.txt`, 0 conflict dirs.** Auditors confirmed the reasoning itself flawless. False-positive. (Lesson: let cloud-sync settle before auditing.) |
| 146, 158, 171 | 9 | Summary "Confirm reading order = rightward", then the exotic **query** op's max-mod-min fails rightward and the chain switches to leftward ("So the actual reading is leftward") | **Rule-driven, mild.** The four-family search confirms a reading from the *example* ops; an exotic *query* op can then revise it — documented behavior, forward-only, narrated explicitly. Reads as a mild inconsistency because the `Note: an exotic operator may change all the rule …` caveat was dropped earlier (approved). **Open: CoT-wording decision** (accept / restore a short caveat / soften the Summary's "Confirm" for exotic-query puzzles). |

Reasoning auditors otherwise unanimous: deterministic, forward-only, self-contained; every boxed answer
matches its derivation; the new corner constructs (exotic+signed-sub, exotic+concat, multi-exotic,
leftward-signed, leading-0/trailing-sign skips) all reason soundly.

## Format sub-10 (24) — one pattern, now fixed

All 24 flag the same legitimate line: `By the distinct operator rule, [ops] cannot be ~sub.` emitted after
the sign-check (the distinctness exclusion when a sign-confirmed ~sub bars the other operators). It is a
**real, common generator line** (present in **265/732** original CoTs, [_gen_eq.py:422]) — the template
simply didn't document it in the sign-check section. **Fixed:** added it to the template's sign-check block.
A re-audit would score these 10.

## Conclusion

The 464 GT-match=True augmentation CoTs are **sound and safe to train on**: reasoning effectively clean
(the only real items are 3 rule-driven reading-revisions awaiting a wording call), format clean after the
one template-doc addition, 0 box-extraction issues. Single-reviewer coverage per axis; for higher assurance
add a 3-vote pass on the exotic/corner classes.

Open items: (1) the exotic reading-revision wording decision; (2) build the training CSV (source `260607_NE_aug`).
