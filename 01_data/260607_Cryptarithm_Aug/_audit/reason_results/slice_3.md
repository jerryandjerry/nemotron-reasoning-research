| id | score | reasons |
|---|---|---|
| 003311 | 8 | Query operator is unseen in the examples; identification rests on an explicit heuristic GUESS ('no 4-digit rhs -> concat') rather than a deduction, and the CoT skips validating the cipher/example arithmetic entirely. Premises (op unseen, no 4-digit rhs) verified true; chain is forward-only, consistent, and the a||b concat answer is computed correctly. |
| 003317 | 8 | Query operator is unseen in the examples; identification rests on an explicit heuristic GUESS ('no 4-digit rhs -> concat') rather than a deduction, and the CoT skips validating the cipher/example arithmetic entirely. Premises (op unseen, no 4-digit rhs) verified true; chain is forward-only, consistent, and the a||b concat answer is computed correctly. |
| 003321 | 8 | Query operator is unseen in the examples; identification rests on an explicit heuristic GUESS ('no 4-digit rhs -> concat') rather than a deduction, and the CoT skips validating the cipher/example arithmetic entirely. Premises (op unseen, no 4-digit rhs) verified true; chain is forward-only, consistent, and the a||b concat answer is computed correctly. |
| 003322 | 8 | Query operator is unseen in the examples; identification rests on an explicit heuristic GUESS ('no 4-digit rhs -> concat') rather than a deduction, and the CoT skips validating the cipher/example arithmetic entirely. Premises (op unseen, no 4-digit rhs) verified true; chain is forward-only, consistent, and the a||b concat answer is computed correctly. |
| 003323 | 8 | Query operator is unseen in the examples; identification rests on an explicit heuristic GUESS ('no 4-digit rhs -> concat') rather than a deduction, and the CoT skips validating the cipher/example arithmetic entirely. Premises (op unseen, no 4-digit rhs) verified true; chain is forward-only, consistent, and the a||b concat answer is computed correctly. |
| 003325 | 8 | Query operator is unseen in the examples; identification rests on an explicit heuristic GUESS ('no 4-digit rhs -> concat') rather than a deduction, and the CoT skips validating the cipher/example arithmetic entirely. Premises (op unseen, no 4-digit rhs) verified true; chain is forward-only, consistent, and the a||b concat answer is computed correctly. |
| 003326 | 8 | Query operator is unseen in the examples; identification rests on an explicit heuristic GUESS ('no 4-digit rhs -> concat') rather than a deduction, and the CoT skips validating the cipher/example arithmetic entirely. Premises (op unseen, no 4-digit rhs) verified true; chain is forward-only, consistent, and the a||b concat answer is computed correctly. |
| 003329 | 8 | Query operator is unseen in the examples; identification rests on an explicit heuristic GUESS ('no 4-digit rhs -> concat') rather than a deduction, and the CoT skips validating the cipher/example arithmetic entirely. Premises (op unseen, no 4-digit rhs) verified true; chain is forward-only, consistent, and the a||b concat answer is computed correctly. |
| 003340 | 8 | Query operator is unseen in the examples; identification rests on an explicit heuristic GUESS ('no 4-digit rhs -> concat') rather than a deduction, and the CoT skips validating the cipher/example arithmetic entirely. Premises (op unseen, no 4-digit rhs) verified true; chain is forward-only, consistent, and the a||b concat answer is computed correctly. |
| 003346 | 8 | Query operator is unseen in the examples; identification rests on an explicit heuristic GUESS ('no 4-digit rhs -> concat') rather than a deduction, and the CoT skips validating the cipher/example arithmetic entirely. Premises (op unseen, no 4-digit rhs) verified true; chain is forward-only, consistent, and the a||b concat answer is computed correctly. |
| 003350 | 8 | Query operator is unseen in the examples; identification rests on an explicit heuristic GUESS ('no 4-digit rhs -> concat') rather than a deduction, and the CoT skips validating the cipher/example arithmetic entirely. Premises (op unseen, no 4-digit rhs) verified true; chain is forward-only, consistent, and the a||b concat answer is computed correctly. |
| 003351 | 8 | Query operator is unseen in the examples; identification rests on an explicit heuristic GUESS ('no 4-digit rhs -> concat') rather than a deduction, and the CoT skips validating the cipher/example arithmetic entirely. Premises (op unseen, no 4-digit rhs) verified true; chain is forward-only, consistent, and the a||b concat answer is computed correctly. |
| 003353 | 8 | Query operator is unseen in the examples; identification rests on an explicit heuristic GUESS ('no 4-digit rhs -> concat') rather than a deduction, and the CoT skips validating the cipher/example arithmetic entirely. Premises (op unseen, no 4-digit rhs) verified true; chain is forward-only, consistent, and the a||b concat answer is computed correctly. |
| 003361 | 8 | Query operator is unseen in the examples; identification rests on an explicit heuristic GUESS ('no 4-digit rhs -> concat') rather than a deduction, and the CoT skips validating the cipher/example arithmetic entirely. Premises (op unseen, no 4-digit rhs) verified true; chain is forward-only, consistent, and the a||b concat answer is computed correctly. |
| 003366 | 8 | Query operator is unseen in the examples; identification rests on an explicit heuristic GUESS ('no 4-digit rhs -> concat') rather than a deduction, and the CoT skips validating the cipher/example arithmetic entirely. Premises (op unseen, no 4-digit rhs) verified true; chain is forward-only, consistent, and the a||b concat answer is computed correctly. |
| 003376 | 8 | Query operator is unseen in the examples; identification rests on an explicit heuristic GUESS ('no 4-digit rhs -> concat') rather than a deduction, and the CoT skips validating the cipher/example arithmetic entirely. Premises (op unseen, no 4-digit rhs) verified true; chain is forward-only, consistent, and the a||b concat answer is computed correctly. |
| 003380 | 8 | Query operator is unseen in the examples; identification rests on an explicit heuristic GUESS ('no 4-digit rhs -> concat') rather than a deduction, and the CoT skips validating the cipher/example arithmetic entirely. Premises (op unseen, no 4-digit rhs) verified true; chain is forward-only, consistent, and the a||b concat answer is computed correctly. |
| 003386 | 8 | Query operator is unseen in the examples; identification rests on an explicit heuristic GUESS ('no 4-digit rhs -> concat') rather than a deduction, and the CoT skips validating the cipher/example arithmetic entirely. Premises (op unseen, no 4-digit rhs) verified true; chain is forward-only, consistent, and the a||b concat answer is computed correctly. |
| 003388 | 8 | Query operator is unseen in the examples; identification rests on an explicit heuristic GUESS ('no 4-digit rhs -> concat') rather than a deduction, and the CoT skips validating the cipher/example arithmetic entirely. Premises (op unseen, no 4-digit rhs) verified true; chain is forward-only, consistent, and the a||b concat answer is computed correctly. |
| 003391 | 8 | Query operator is unseen in the examples; identification rests on an explicit heuristic GUESS ('no 4-digit rhs -> concat') rather than a deduction, and the CoT skips validating the cipher/example arithmetic entirely. Premises (op unseen, no 4-digit rhs) verified true; chain is forward-only, consistent, and the a||b concat answer is computed correctly. |
| 003393 | 8 | Query operator is unseen in the examples; identification rests on an explicit heuristic GUESS ('no 4-digit rhs -> concat') rather than a deduction, and the CoT skips validating the cipher/example arithmetic entirely. Premises (op unseen, no 4-digit rhs) verified true; chain is forward-only, consistent, and the a||b concat answer is computed correctly. |
| 003394 | 8 | Query operator is unseen in the examples; identification rests on an explicit heuristic GUESS ('no 4-digit rhs -> concat') rather than a deduction, and the CoT skips validating the cipher/example arithmetic entirely. Premises (op unseen, no 4-digit rhs) verified true; chain is forward-only, consistent, and the a||b concat answer is computed correctly. |
| 003396 | 8 | Query operator is unseen in the examples; identification rests on an explicit heuristic GUESS ('no 4-digit rhs -> concat') rather than a deduction, and the CoT skips validating the cipher/example arithmetic entirely. Premises (op unseen, no 4-digit rhs) verified true; chain is forward-only, consistent, and the a||b concat answer is computed correctly. |
| 003397 | 8 | Query operator is unseen in the examples; identification rests on an explicit heuristic GUESS ('no 4-digit rhs -> concat') rather than a deduction, and the CoT skips validating the cipher/example arithmetic entirely. Premises (op unseen, no 4-digit rhs) verified true; chain is forward-only, consistent, and the a||b concat answer is computed correctly. |
| 003398 | 8 | Query operator is unseen in the examples; identification rests on an explicit heuristic GUESS ('no 4-digit rhs -> concat') rather than a deduction, and the CoT skips validating the cipher/example arithmetic entirely. Premises (op unseen, no 4-digit rhs) verified true; chain is forward-only, consistent, and the a||b concat answer is computed correctly. |
| 003405 | 8 | Query operator is unseen in the examples; identification rests on an explicit heuristic GUESS ('no 4-digit rhs -> concat') rather than a deduction, and the CoT skips validating the cipher/example arithmetic entirely. Premises (op unseen, no 4-digit rhs) verified true; chain is forward-only, consistent, and the a||b concat answer is computed correctly. |
| 003407 | 8 | Query operator is unseen in the examples; identification rests on an explicit heuristic GUESS ('no 4-digit rhs -> concat') rather than a deduction, and the CoT skips validating the cipher/example arithmetic entirely. Premises (op unseen, no 4-digit rhs) verified true; chain is forward-only, consistent, and the a||b concat answer is computed correctly. |
| 003408 | 8 | Query operator is unseen in the examples; identification rests on an explicit heuristic GUESS ('no 4-digit rhs -> concat') rather than a deduction, and the CoT skips validating the cipher/example arithmetic entirely. Premises (op unseen, no 4-digit rhs) verified true; chain is forward-only, consistent, and the a||b concat answer is computed correctly. |
| 003426 | 8 | Query operator is unseen in the examples; identification rests on an explicit heuristic GUESS ('no 4-digit rhs -> concat') rather than a deduction, and the CoT skips validating the cipher/example arithmetic entirely. Premises (op unseen, no 4-digit rhs) verified true; chain is forward-only, consistent, and the a||b concat answer is computed correctly. |
| 003439 | 8 | Query operator is unseen in the examples; identification rests on an explicit heuristic GUESS ('no 4-digit rhs -> concat') rather than a deduction, and the CoT skips validating the cipher/example arithmetic entirely. Premises (op unseen, no 4-digit rhs) verified true; chain is forward-only, consistent, and the a||b concat answer is computed correctly. |
| 003445 | 8 | Query operator is unseen in the examples; identification rests on an explicit heuristic GUESS ('no 4-digit rhs -> concat') rather than a deduction, and the CoT skips validating the cipher/example arithmetic entirely. Premises (op unseen, no 4-digit rhs) verified true; chain is forward-only, consistent, and the a||b concat answer is computed correctly. |
| 003450 | 8 | Query operator is unseen in the examples; identification rests on an explicit heuristic GUESS ('no 4-digit rhs -> concat') rather than a deduction, and the CoT skips validating the cipher/example arithmetic entirely. Premises (op unseen, no 4-digit rhs) verified true; chain is forward-only, consistent, and the a||b concat answer is computed correctly. |
| 003460 | 8 | Query operator is unseen in the examples; identification rests on an explicit heuristic GUESS ('no 4-digit rhs -> concat') rather than a deduction, and the CoT skips validating the cipher/example arithmetic entirely. Premises (op unseen, no 4-digit rhs) verified true; chain is forward-only, consistent, and the a||b concat answer is computed correctly. |
| 003468 | 8 | Query operator is unseen in the examples; identification rests on an explicit heuristic GUESS ('no 4-digit rhs -> concat') rather than a deduction, and the CoT skips validating the cipher/example arithmetic entirely. Premises (op unseen, no 4-digit rhs) verified true; chain is forward-only, consistent, and the a||b concat answer is computed correctly. |
| 003470 | 8 | Query operator is unseen in the examples; identification rests on an explicit heuristic GUESS ('no 4-digit rhs -> concat') rather than a deduction, and the CoT skips validating the cipher/example arithmetic entirely. Premises (op unseen, no 4-digit rhs) verified true; chain is forward-only, consistent, and the a||b concat answer is computed correctly. |
| 003479 | 8 | Query operator is unseen in the examples; identification rests on an explicit heuristic GUESS ('no 4-digit rhs -> concat') rather than a deduction, and the CoT skips validating the cipher/example arithmetic entirely. Premises (op unseen, no 4-digit rhs) verified true; chain is forward-only, consistent, and the a||b concat answer is computed correctly. |
| 003493 | 8 | Query operator is unseen in the examples; identification rests on an explicit heuristic GUESS ('no 4-digit rhs -> concat') rather than a deduction, and the CoT skips validating the cipher/example arithmetic entirely. Premises (op unseen, no 4-digit rhs) verified true; chain is forward-only, consistent, and the a||b concat answer is computed correctly. |
| 003496 | 8 | Query operator is unseen in the examples; identification rests on an explicit heuristic GUESS ('no 4-digit rhs -> concat') rather than a deduction, and the CoT skips validating the cipher/example arithmetic entirely. Premises (op unseen, no 4-digit rhs) verified true; chain is forward-only, consistent, and the a||b concat answer is computed correctly. |
| 003509 | 8 | Query operator is unseen in the examples; identification rests on an explicit heuristic GUESS ('no 4-digit rhs -> concat') rather than a deduction, and the CoT skips validating the cipher/example arithmetic entirely. Premises (op unseen, no 4-digit rhs) verified true; chain is forward-only, consistent, and the a||b concat answer is computed correctly. |
| 003510 | 8 | Query operator is unseen in the examples; identification rests on an explicit heuristic GUESS ('no 4-digit rhs -> concat') rather than a deduction, and the CoT skips validating the cipher/example arithmetic entirely. Premises (op unseen, no 4-digit rhs) verified true; chain is forward-only, consistent, and the a||b concat answer is computed correctly. |
| 003511 | 8 | Query operator is unseen in the examples; identification rests on an explicit heuristic GUESS ('no 4-digit rhs -> concat') rather than a deduction, and the CoT skips validating the cipher/example arithmetic entirely. Premises (op unseen, no 4-digit rhs) verified true; chain is forward-only, consistent, and the a||b concat answer is computed correctly. |
| 003521 | 8 | Query operator is unseen in the examples; identification rests on an explicit heuristic GUESS ('no 4-digit rhs -> concat') rather than a deduction, and the CoT skips validating the cipher/example arithmetic entirely. Premises (op unseen, no 4-digit rhs) verified true; chain is forward-only, consistent, and the a||b concat answer is computed correctly. |
| 003525 | 8 | Query operator is unseen in the examples; identification rests on an explicit heuristic GUESS ('no 4-digit rhs -> concat') rather than a deduction, and the CoT skips validating the cipher/example arithmetic entirely. Premises (op unseen, no 4-digit rhs) verified true; chain is forward-only, consistent, and the a||b concat answer is computed correctly. |
| 003527 | 8 | Query operator is unseen in the examples; identification rests on an explicit heuristic GUESS ('no 4-digit rhs -> concat') rather than a deduction, and the CoT skips validating the cipher/example arithmetic entirely. Premises (op unseen, no 4-digit rhs) verified true; chain is forward-only, consistent, and the a||b concat answer is computed correctly. |
| 003530 | 8 | Query operator is unseen in the examples; identification rests on an explicit heuristic GUESS ('no 4-digit rhs -> concat') rather than a deduction, and the CoT skips validating the cipher/example arithmetic entirely. Premises (op unseen, no 4-digit rhs) verified true; chain is forward-only, consistent, and the a||b concat answer is computed correctly. |
| 003536 | 8 | Query operator is unseen in the examples; identification rests on an explicit heuristic GUESS ('no 4-digit rhs -> concat') rather than a deduction, and the CoT skips validating the cipher/example arithmetic entirely. Premises (op unseen, no 4-digit rhs) verified true; chain is forward-only, consistent, and the a||b concat answer is computed correctly. |
| 003537 | 8 | Query operator is unseen in the examples; identification rests on an explicit heuristic GUESS ('no 4-digit rhs -> concat') rather than a deduction, and the CoT skips validating the cipher/example arithmetic entirely. Premises (op unseen, no 4-digit rhs) verified true; chain is forward-only, consistent, and the a||b concat answer is computed correctly. |
| 003539 | 8 | Query operator is unseen in the examples; identification rests on an explicit heuristic GUESS ('no 4-digit rhs -> concat') rather than a deduction, and the CoT skips validating the cipher/example arithmetic entirely. Premises (op unseen, no 4-digit rhs) verified true; chain is forward-only, consistent, and the a||b concat answer is computed correctly. |
| 003545 | 8 | Query operator is unseen in the examples; identification rests on an explicit heuristic GUESS ('no 4-digit rhs -> concat') rather than a deduction, and the CoT skips validating the cipher/example arithmetic entirely. Premises (op unseen, no 4-digit rhs) verified true; chain is forward-only, consistent, and the a||b concat answer is computed correctly. |
| 003559 | 8 | Query operator is unseen in the examples; identification rests on an explicit heuristic GUESS ('no 4-digit rhs -> concat') rather than a deduction, and the CoT skips validating the cipher/example arithmetic entirely. Premises (op unseen, no 4-digit rhs) verified true; chain is forward-only, consistent, and the a||b concat answer is computed correctly. |
| 003567 | 8 | Query operator is unseen in the examples; identification rests on an explicit heuristic GUESS ('no 4-digit rhs -> concat') rather than a deduction, and the CoT skips validating the cipher/example arithmetic entirely. Premises (op unseen, no 4-digit rhs) verified true; chain is forward-only, consistent, and the a||b concat answer is computed correctly. |
| 003572 | 8 | Query operator is unseen in the examples; identification rests on an explicit heuristic GUESS ('no 4-digit rhs -> concat') rather than a deduction, and the CoT skips validating the cipher/example arithmetic entirely. Premises (op unseen, no 4-digit rhs) verified true; chain is forward-only, consistent, and the a||b concat answer is computed correctly. |
| 003578 | 8 | Query operator is unseen in the examples; identification rests on an explicit heuristic GUESS ('no 4-digit rhs -> concat') rather than a deduction, and the CoT skips validating the cipher/example arithmetic entirely. Premises (op unseen, no 4-digit rhs) verified true; chain is forward-only, consistent, and the a||b concat answer is computed correctly. |
| 003597 | 8 | Query operator is unseen in the examples; identification rests on an explicit heuristic GUESS ('no 4-digit rhs -> concat') rather than a deduction, and the CoT skips validating the cipher/example arithmetic entirely. Premises (op unseen, no 4-digit rhs) verified true; chain is forward-only, consistent, and the a||b concat answer is computed correctly. |
| 003600 | 8 | Query operator is unseen in the examples; identification rests on an explicit heuristic GUESS ('no 4-digit rhs -> concat') rather than a deduction, and the CoT skips validating the cipher/example arithmetic entirely. Premises (op unseen, no 4-digit rhs) verified true; chain is forward-only, consistent, and the a||b concat answer is computed correctly. |
| 003602 | 8 | Query operator is unseen in the examples; identification rests on an explicit heuristic GUESS ('no 4-digit rhs -> concat') rather than a deduction, and the CoT skips validating the cipher/example arithmetic entirely. Premises (op unseen, no 4-digit rhs) verified true; chain is forward-only, consistent, and the a||b concat answer is computed correctly. |
| 003608 | 8 | Query operator is unseen in the examples; identification rests on an explicit heuristic GUESS ('no 4-digit rhs -> concat') rather than a deduction, and the CoT skips validating the cipher/example arithmetic entirely. Premises (op unseen, no 4-digit rhs) verified true; chain is forward-only, consistent, and the a||b concat answer is computed correctly. |
| 003614 | 8 | Query operator is unseen in the examples; identification rests on an explicit heuristic GUESS ('no 4-digit rhs -> concat') rather than a deduction, and the CoT skips validating the cipher/example arithmetic entirely. Premises (op unseen, no 4-digit rhs) verified true; chain is forward-only, consistent, and the a||b concat answer is computed correctly. |
| 003618 | 8 | Query operator is unseen in the examples; identification rests on an explicit heuristic GUESS ('no 4-digit rhs -> concat') rather than a deduction, and the CoT skips validating the cipher/example arithmetic entirely. Premises (op unseen, no 4-digit rhs) verified true; chain is forward-only, consistent, and the a||b concat answer is computed correctly. |
| 003622 | 8 | Query operator is unseen in the examples; identification rests on an explicit heuristic GUESS ('no 4-digit rhs -> concat') rather than a deduction, and the CoT skips validating the cipher/example arithmetic entirely. Premises (op unseen, no 4-digit rhs) verified true; chain is forward-only, consistent, and the a||b concat answer is computed correctly. |
| 003623 | 8 | Query operator is unseen in the examples; identification rests on an explicit heuristic GUESS ('no 4-digit rhs -> concat') rather than a deduction, and the CoT skips validating the cipher/example arithmetic entirely. Premises (op unseen, no 4-digit rhs) verified true; chain is forward-only, consistent, and the a||b concat answer is computed correctly. |
| 003634 | 8 | Query operator is unseen in the examples; identification rests on an explicit heuristic GUESS ('no 4-digit rhs -> concat') rather than a deduction, and the CoT skips validating the cipher/example arithmetic entirely. Premises (op unseen, no 4-digit rhs) verified true; chain is forward-only, consistent, and the a||b concat answer is computed correctly. |
| 003637 | 8 | Query operator is unseen in the examples; identification rests on an explicit heuristic GUESS ('no 4-digit rhs -> concat') rather than a deduction, and the CoT skips validating the cipher/example arithmetic entirely. Premises (op unseen, no 4-digit rhs) verified true; chain is forward-only, consistent, and the a||b concat answer is computed correctly. |
| 003639 | 8 | Query operator is unseen in the examples; identification rests on an explicit heuristic GUESS ('no 4-digit rhs -> concat') rather than a deduction, and the CoT skips validating the cipher/example arithmetic entirely. Premises (op unseen, no 4-digit rhs) verified true; chain is forward-only, consistent, and the a||b concat answer is computed correctly. |
| 003640 | 8 | Query operator is unseen in the examples; identification rests on an explicit heuristic GUESS ('no 4-digit rhs -> concat') rather than a deduction, and the CoT skips validating the cipher/example arithmetic entirely. Premises (op unseen, no 4-digit rhs) verified true; chain is forward-only, consistent, and the a||b concat answer is computed correctly. |
| 003644 | 8 | Query operator is unseen in the examples; identification rests on an explicit heuristic GUESS ('no 4-digit rhs -> concat') rather than a deduction, and the CoT skips validating the cipher/example arithmetic entirely. Premises (op unseen, no 4-digit rhs) verified true; chain is forward-only, consistent, and the a||b concat answer is computed correctly. |
| 003647 | 8 | Query operator is unseen in the examples; identification rests on an explicit heuristic GUESS ('no 4-digit rhs -> concat') rather than a deduction, and the CoT skips validating the cipher/example arithmetic entirely. Premises (op unseen, no 4-digit rhs) verified true; chain is forward-only, consistent, and the a||b concat answer is computed correctly. |
| 003648 | 8 | Query operator is unseen in the examples; identification rests on an explicit heuristic GUESS ('no 4-digit rhs -> concat') rather than a deduction, and the CoT skips validating the cipher/example arithmetic entirely. Premises (op unseen, no 4-digit rhs) verified true; chain is forward-only, consistent, and the a||b concat answer is computed correctly. |
| 003650 | 8 | Query operator is unseen in the examples; identification rests on an explicit heuristic GUESS ('no 4-digit rhs -> concat') rather than a deduction, and the CoT skips validating the cipher/example arithmetic entirely. Premises (op unseen, no 4-digit rhs) verified true; chain is forward-only, consistent, and the a||b concat answer is computed correctly. |
