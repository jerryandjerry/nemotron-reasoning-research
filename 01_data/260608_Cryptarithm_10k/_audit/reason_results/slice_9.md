| id | score | reasons |
|---|---|---|
| crypt10k_009011 | 8 | Query operator `<` (h) is unseen in examples; answer hinges on the concat guess (line "I'll guess h = a∥b"), not forced. Otherwise forward-only, internally consistent, boxed `:)}%` matches concat. |
| crypt10k_009023 | 8 | Unseen query op `<`; load-bearing concat guess (line 54). Reasoning honest and consistent; boxed `![[[` = concat. |
| crypt10k_009063 | 8 | Unseen query op `%`; answer set by concat fallback, not deduced. Mechanics correct; boxed `!/{/` matches. |
| crypt10k_009072 | 8 | Unseen query op `"`; concat guess load-bearing (line 58). Consistent; boxed `/%//` matches. |
| crypt10k_009078 | 8 | Unseen query op `{`; answer rests on concat guess. Forward-only and consistent; boxed `:\?>` matches. |
| crypt10k_009105 | 8 | Unseen query op `{`; concat fallback load-bearing. Mechanics correct; boxed `}[[\` matches. |
| crypt10k_009126 | 8 | Unseen query op `` ` ``; answer is the admitted concat guess. Consistent; boxed `(\""` matches. |
| crypt10k_009148 | 8 | Unseen query op `/`; concat guess load-bearing (line 51). Consistent; boxed `?{{?` matches. |
| crypt10k_009195 | 8 | Unseen query op `:`; answer set by concat fallback. Consistent; boxed `{$&$` matches. |
| crypt10k_009201 | 8 | Unseen query op `%`; concat guess load-bearing. Consistent; boxed `){#'` matches. |
| crypt10k_009213 | 8 | Unseen query op `<`; answer rests on concat guess. Consistent; boxed `\{'}` matches. |
| crypt10k_009227 | 8 | Unseen query op `!`; concat fallback load-bearing. Consistent; boxed ` ``/| ` matches. |
| crypt10k_009232 | 8 | Unseen query op `/`; answer is concat guess, not forced. Consistent; boxed `|^%>` matches. |
| crypt10k_009249 | 8 | Unseen query op `{`; concat guess load-bearing. Consistent; boxed `}|}'` matches. |
| crypt10k_009258 | 8 | Unseen query op `` ` ``; answer set by concat fallback. Consistent; boxed `!>?]` matches. |
| crypt10k_009270 | 8 | Only 2 distinct ops (`+`,`|`), query op `|` still unseen; concat guess load-bearing. Consistent; boxed `@(@!` matches. |
| crypt10k_009275 | 8 | Unseen query op `#`; concat fallback load-bearing. Consistent; boxed `}{"{` matches. |
| crypt10k_009284 | 8 | Unseen query op `%`; answer is the admitted concat guess. Consistent; boxed `(#(!` matches. |
| crypt10k_009286 | 8 | Unseen query op `!`; concat guess load-bearing. Consistent; boxed `>({'` matches. |
| crypt10k_009308 | 8 | Unseen query op `@`; answer set by concat fallback. Consistent; boxed `(>(#` matches. |
| crypt10k_009310 | 8 | Unseen query op `` ` ``; concat guess load-bearing. Consistent; boxed `'@@#` matches. |
| crypt10k_009318 | 8 | Unseen query op `^`; answer rests on concat guess. Consistent; boxed `#(!#` matches. |
| crypt10k_009357 | 8 | Unseen query op `:`; all examples use `+`; concat guess load-bearing. Consistent; boxed `}&@&` matches. |
| crypt10k_009367 | 8 | Unseen query op `@`; concat fallback load-bearing. Consistent; boxed `"[)#` matches. |
| crypt10k_009396 | 8 | Unseen query op `<`; answer is concat guess. Consistent; boxed `[>:{` matches. |
| crypt10k_009410 | 8 | Unseen query op `@`; concat guess load-bearing. Consistent; boxed `/>/^` matches. |
| crypt10k_009412 | 8 | Unseen query op `>`; answer set by concat fallback. Consistent; boxed `%"&%` matches. |
| crypt10k_009424 | 8 | Unseen query op `>`; concat guess load-bearing. Consistent; boxed `<%<%` matches. |
| crypt10k_009434 | 8 | Unseen query op `|`; answer rests on concat guess. Consistent; boxed `@`#<` matches. |
| crypt10k_009445 | 8 | Unseen query op `'`; concat fallback load-bearing. Consistent; boxed `/#/|` matches. |
| crypt10k_009448 | 8 | Unseen query op `?`; answer is the admitted concat guess. Consistent; boxed `\%^]` matches. |
| crypt10k_009449 | 8 | Unseen query op `/`; concat guess load-bearing. Consistent; boxed `(["<` matches. |
| crypt10k_009463 | 8 | Unseen query op `^`; answer set by concat fallback. Consistent; boxed `{}`'` matches. |
| crypt10k_009465 | 8 | Unseen query op `(`; concat guess load-bearing. Consistent; boxed `!#?!` matches. |
| crypt10k_009470 | 8 | Unseen query op `` ` ``; answer rests on concat guess. Consistent; boxed `^$|$` matches. |
| crypt10k_009474 | 8 | Unseen query op `` ` ``; concat fallback load-bearing. Consistent; boxed `/</'` matches. |
| crypt10k_009486 | 8 | Unseen query op `@`; answer is concat guess. Consistent; boxed `\$:`` matches. |
| crypt10k_009489 | 8 | Unseen query op `#`; concat guess load-bearing. Consistent; boxed `''([` matches. |
| crypt10k_009507 | 8 | Unseen query op `]`; answer set by concat fallback. Consistent; boxed `?{[^` matches. |
| crypt10k_009511 | 8 | Unseen query op `<`; concat guess load-bearing. Consistent; boxed `@#''` matches. |
| crypt10k_009555 | 8 | Unseen query op `"`; answer rests on concat guess. Consistent; boxed `!}%|` matches. |
| crypt10k_009563 | 8 | Unseen query op `:`; concat fallback load-bearing. Consistent; boxed `$"$$` matches. |
| crypt10k_009573 | 8 | Unseen query op `<`; answer is the admitted concat guess. Consistent; boxed `:[}|` matches. |
| crypt10k_009596 | 8 | Unseen query op `` ` ``; concat guess load-bearing (line 62). Consistent; boxed `?}!@` matches. |
| crypt10k_009622 | 8 | Unseen query op `)`; answer set by concat fallback. Consistent; boxed `!@\$` matches. |
| crypt10k_009655 | 8 | Unseen query op `@`; concat guess load-bearing. Consistent; boxed `&$[/` matches. |
| crypt10k_009684 | 8 | Unseen query op `%`; answer rests on concat guess. Consistent; boxed `^@])` matches. |
| crypt10k_009705 | 8 | Unseen query op `[`; concat fallback load-bearing. Consistent; boxed `?\?>` matches. |
| crypt10k_009711 | 8 | Unseen query op `{`; all examples use `(`; concat guess load-bearing. Consistent; boxed `!%^^` matches. |
| crypt10k_009730 | 8 | Unseen query op `"`; answer is the admitted concat guess. Consistent; boxed `#?$?` matches. |
| crypt10k_009762 | 8 | Unseen query op `` ` ``; concat guess load-bearing. Consistent; boxed `${{$` matches. |
| crypt10k_009785 | 8 | Unseen query op `/`; answer set by concat fallback. Consistent; boxed `''\%` matches. |
| crypt10k_009793 | 8 | Unseen query op `?`; concat guess load-bearing. Consistent; boxed `>}#^` matches. |
| crypt10k_009798 | 8 | Unseen query op `%`; answer rests on concat guess. Consistent; boxed `` `!<! `` matches. |
| crypt10k_009810 | 8 | Unseen query op `@`; all examples use `+`; concat guess load-bearing. Consistent; boxed `]$\'` matches. |
| crypt10k_009813 | 8 | Unseen query op `#`; answer is the admitted concat guess. Consistent; boxed `/]]]` matches. |
| crypt10k_009821 | 8 | Unseen query op `?`; concat guess load-bearing. Consistent; boxed `)^#}` matches. |
| crypt10k_009830 | 8 | Unseen query op `@`; answer set by concat fallback. Consistent; boxed `\$<#` matches. |
| crypt10k_009841 | 8 | Unseen query op `>`; concat guess load-bearing. Consistent; boxed `:\/\` matches. |
| crypt10k_009868 | 8 | Unseen query op `!`; answer rests on concat guess. Consistent; boxed `&&#`` matches. |
| crypt10k_009876 | 8 | Unseen query op `` ` ``; concat fallback load-bearing. Consistent; boxed `"\\[` matches. |
| crypt10k_009880 | 8 | Unseen query op `/`; answer is the admitted concat guess. Consistent; boxed `[|#&` matches. |
| crypt10k_009882 | 8 | Unseen query op `/`; concat guess load-bearing. Consistent; boxed `|{{|` matches. |
| crypt10k_009892 | 8 | Unseen query op `<`; answer set by concat fallback. Consistent; boxed `|{`{` matches. |
| crypt10k_009917 | 8 | Unseen query op `(`; concat guess load-bearing. Consistent; boxed `]{){` matches. |
| crypt10k_009923 | 8 | Unseen query op `:`; answer rests on concat guess. Consistent; boxed `$?$\` matches. |
| crypt10k_009946 | 8 | Unseen query op `)`; concat fallback load-bearing. Consistent; boxed `#/"!` matches. |
| crypt10k_009956 | 8 | Unseen query op `!`; answer is the admitted concat guess. Consistent; boxed `%$[\` matches. |
| crypt10k_009969 | 8 | Unseen query op `|`; concat guess load-bearing. Consistent; boxed `>">(` matches. |
| crypt10k_009983 | 8 | Unseen query op `` ` ``; answer set by concat fallback. Consistent; boxed `??((` matches. |
