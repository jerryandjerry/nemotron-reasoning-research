| id | score | deviations |
|---|---|---|
| crypt10k_006004 | 9 | non-template short-circuit lines: 'Note: <op> is the QUERY operator ... Proceed to answer directly.' + Summary prepends 'Query operator <op> = ..., no need to solve the other unknowns;' (template Summary is bare 'Confirm reading order = ...') |
| crypt10k_006022 | 9 | non-template short-circuit lines: 'Note: <op> is the QUERY operator ... Proceed to answer directly.' + Summary prepends 'Query operator <op> = ..., no need to solve the other unknowns;' (template Summary is bare 'Confirm reading order = ...') |
| crypt10k_006038 | 9 | non-template short-circuit lines: 'Note: <op> is the QUERY operator ... Proceed to answer directly.' + Summary prepends 'Query operator <op> = ..., no need to solve the other unknowns;' (template Summary is bare 'Confirm reading order = ...') |
| crypt10k_006040 | 8 | non-template short-circuit lines: 'Note: <op> is the QUERY operator ... Proceed to answer directly.' + Summary prepends 'Query operator <op> = ..., no need to solve the other unknowns;' (template Summary is bare 'Confirm reading order = ...'); boxed answer contains a literal brace ('}>>:') -> first brace closes the box early (render ambiguity; not a generator rule violation) |
| crypt10k_006047 | 8 | non-template short-circuit lines: 'Note: <op> is the QUERY operator ... Proceed to answer directly.' + Summary prepends 'Query operator <op> = ..., no need to solve the other unknowns;' (template Summary is bare 'Confirm reading order = ...'); boxed answer contains a literal brace ('{\&"') -> first brace closes the box early (render ambiguity; not a generator rule violation) |
| crypt10k_006052 | 9 | non-template short-circuit lines: 'Note: <op> is the QUERY operator ... Proceed to answer directly.' + Summary prepends 'Query operator <op> = ..., no need to solve the other unknowns;' (template Summary is bare 'Confirm reading order = ...') |
| crypt10k_006080 | 9 | non-template short-circuit lines: 'Note: <op> is the QUERY operator ... Proceed to answer directly.' + Summary prepends 'Query operator <op> = ..., no need to solve the other unknowns;' (template Summary is bare 'Confirm reading order = ...') |
| crypt10k_006084 | 8 | non-template short-circuit lines: 'Note: <op> is the QUERY operator ... Proceed to answer directly.' + Summary prepends 'Query operator <op> = ..., no need to solve the other unknowns;' (template Summary is bare 'Confirm reading order = ...'); boxed answer contains a literal brace ('{}!]') -> first brace closes the box early (render ambiguity; not a generator rule violation) |
| crypt10k_006096 | 9 | non-template short-circuit lines: 'Note: <op> is the QUERY operator ... Proceed to answer directly.' + Summary prepends 'Query operator <op> = ..., no need to solve the other unknowns;' (template Summary is bare 'Confirm reading order = ...') |
| crypt10k_006100 | 9 | non-template short-circuit lines: 'Note: <op> is the QUERY operator ... Proceed to answer directly.' + Summary prepends 'Query operator <op> = ..., no need to solve the other unknowns;' (template Summary is bare 'Confirm reading order = ...') |
| crypt10k_006101 | 9 | non-template short-circuit lines: 'Note: <op> is the QUERY operator ... Proceed to answer directly.' + Summary prepends 'Query operator <op> = ..., no need to solve the other unknowns;' (template Summary is bare 'Confirm reading order = ...') |
| crypt10k_006105 | 9 | non-template short-circuit lines: 'Note: <op> is the QUERY operator ... Proceed to answer directly.' + Summary prepends 'Query operator <op> = ..., no need to solve the other unknowns;' (template Summary is bare 'Confirm reading order = ...') |
| crypt10k_006144 | 9 | non-template short-circuit lines: 'Note: <op> is the QUERY operator ... Proceed to answer directly.' + Summary prepends 'Query operator <op> = ..., no need to solve the other unknowns;' (template Summary is bare 'Confirm reading order = ...') |
| crypt10k_006164 | 8 | non-template short-circuit lines: 'Note: <op> is the QUERY operator ... Proceed to answer directly.' + Summary prepends 'Query operator <op> = ..., no need to solve the other unknowns;' (template Summary is bare 'Confirm reading order = ...'); boxed answer contains a literal brace ('[}]'') -> first brace closes the box early (render ambiguity; not a generator rule violation) |
| crypt10k_006179 | 8 | non-template short-circuit lines: 'Note: <op> is the QUERY operator ... Proceed to answer directly.' + Summary prepends 'Query operator <op> = ..., no need to solve the other unknowns;' (template Summary is bare 'Confirm reading order = ...'); boxed answer contains a literal brace ('{`$)') -> first brace closes the box early (render ambiguity; not a generator rule violation) |
| crypt10k_006196 | 9 | non-template short-circuit lines: 'Note: <op> is the QUERY operator ... Proceed to answer directly.' + Summary prepends 'Query operator <op> = ..., no need to solve the other unknowns;' (template Summary is bare 'Confirm reading order = ...') |
| crypt10k_006211 | 8 | non-template short-circuit lines: 'Note: <op> is the QUERY operator ... Proceed to answer directly.' + Summary prepends 'Query operator <op> = ..., no need to solve the other unknowns;' (template Summary is bare 'Confirm reading order = ...'); boxed answer contains a literal brace ('/}>>') -> first brace closes the box early (render ambiguity; not a generator rule violation) |
| crypt10k_006212 | 9 | non-template short-circuit lines: 'Note: <op> is the QUERY operator ... Proceed to answer directly.' + Summary prepends 'Query operator <op> = ..., no need to solve the other unknowns;' (template Summary is bare 'Confirm reading order = ...') |
| crypt10k_006244 | 9 | non-template short-circuit lines: 'Note: <op> is the QUERY operator ... Proceed to answer directly.' + Summary prepends 'Query operator <op> = ..., no need to solve the other unknowns;' (template Summary is bare 'Confirm reading order = ...') |
| crypt10k_006258 | 9 | non-template short-circuit lines: 'Note: <op> is the QUERY operator ... Proceed to answer directly.' + Summary prepends 'Query operator <op> = ..., no need to solve the other unknowns;' (template Summary is bare 'Confirm reading order = ...') |
| crypt10k_006263 | 9 | non-template short-circuit lines: 'Note: <op> is the QUERY operator ... Proceed to answer directly.' + Summary prepends 'Query operator <op> = ..., no need to solve the other unknowns;' (template Summary is bare 'Confirm reading order = ...') |
| crypt10k_006268 | 9 | non-template short-circuit lines: 'Note: <op> is the QUERY operator ... Proceed to answer directly.' + Summary prepends 'Query operator <op> = ..., no need to solve the other unknowns;' (template Summary is bare 'Confirm reading order = ...') |
| crypt10k_006271 | 9 | non-template short-circuit lines: 'Note: <op> is the QUERY operator ... Proceed to answer directly.' + Summary prepends 'Query operator <op> = ..., no need to solve the other unknowns;' (template Summary is bare 'Confirm reading order = ...') |
| crypt10k_006277 | 9 | non-template short-circuit lines: 'Note: <op> is the QUERY operator ... Proceed to answer directly.' + Summary prepends 'Query operator <op> = ..., no need to solve the other unknowns;' (template Summary is bare 'Confirm reading order = ...') |
| crypt10k_006289 | 9 | non-template short-circuit lines: 'Note: <op> is the QUERY operator ... Proceed to answer directly.' + Summary prepends 'Query operator <op> = ..., no need to solve the other unknowns;' (template Summary is bare 'Confirm reading order = ...') |
| crypt10k_006338 | 9 | non-template short-circuit lines: 'Note: <op> is the QUERY operator ... Proceed to answer directly.' + Summary prepends 'Query operator <op> = ..., no need to solve the other unknowns;' (template Summary is bare 'Confirm reading order = ...') |
| crypt10k_006341 | 8 | non-template short-circuit lines: 'Note: <op> is the QUERY operator ... Proceed to answer directly.' + Summary prepends 'Query operator <op> = ..., no need to solve the other unknowns;' (template Summary is bare 'Confirm reading order = ...'); boxed answer contains a literal brace ('<`<{') -> first brace closes the box early (render ambiguity; not a generator rule violation) |
| crypt10k_006359 | 9 | non-template short-circuit lines: 'Note: <op> is the QUERY operator ... Proceed to answer directly.' + Summary prepends 'Query operator <op> = ..., no need to solve the other unknowns;' (template Summary is bare 'Confirm reading order = ...') |
| crypt10k_006364 | 9 | non-template short-circuit lines: 'Note: <op> is the QUERY operator ... Proceed to answer directly.' + Summary prepends 'Query operator <op> = ..., no need to solve the other unknowns;' (template Summary is bare 'Confirm reading order = ...') |
| crypt10k_006377 | 9 | non-template short-circuit lines: 'Note: <op> is the QUERY operator ... Proceed to answer directly.' + Summary prepends 'Query operator <op> = ..., no need to solve the other unknowns;' (template Summary is bare 'Confirm reading order = ...') |
| crypt10k_006385 | 9 | non-template short-circuit lines: 'Note: <op> is the QUERY operator ... Proceed to answer directly.' + Summary prepends 'Query operator <op> = ..., no need to solve the other unknowns;' (template Summary is bare 'Confirm reading order = ...') |
| crypt10k_006416 | 9 | non-template short-circuit lines: 'Note: <op> is the QUERY operator ... Proceed to answer directly.' + Summary prepends 'Query operator <op> = ..., no need to solve the other unknowns;' (template Summary is bare 'Confirm reading order = ...') |
| crypt10k_006441 | 8 | non-template short-circuit lines: 'Note: <op> is the QUERY operator ... Proceed to answer directly.' + Summary prepends 'Query operator <op> = ..., no need to solve the other unknowns;' (template Summary is bare 'Confirm reading order = ...'); boxed answer contains a literal brace ('[&})') -> first brace closes the box early (render ambiguity; not a generator rule violation) |
| crypt10k_006445 | 9 | non-template short-circuit lines: 'Note: <op> is the QUERY operator ... Proceed to answer directly.' + Summary prepends 'Query operator <op> = ..., no need to solve the other unknowns;' (template Summary is bare 'Confirm reading order = ...') |
| crypt10k_006456 | 8 | non-template short-circuit lines: 'Note: <op> is the QUERY operator ... Proceed to answer directly.' + Summary prepends 'Query operator <op> = ..., no need to solve the other unknowns;' (template Summary is bare 'Confirm reading order = ...'); boxed answer contains a literal brace ('#{`'') -> first brace closes the box early (render ambiguity; not a generator rule violation) |
| crypt10k_006469 | 9 | non-template short-circuit lines: 'Note: <op> is the QUERY operator ... Proceed to answer directly.' + Summary prepends 'Query operator <op> = ..., no need to solve the other unknowns;' (template Summary is bare 'Confirm reading order = ...') |
| crypt10k_006487 | 8 | non-template short-circuit lines: 'Note: <op> is the QUERY operator ... Proceed to answer directly.' + Summary prepends 'Query operator <op> = ..., no need to solve the other unknowns;' (template Summary is bare 'Confirm reading order = ...'); boxed answer contains a literal brace ('^%>{') -> first brace closes the box early (render ambiguity; not a generator rule violation) |
| crypt10k_006491 | 9 | non-template short-circuit lines: 'Note: <op> is the QUERY operator ... Proceed to answer directly.' + Summary prepends 'Query operator <op> = ..., no need to solve the other unknowns;' (template Summary is bare 'Confirm reading order = ...') |
| crypt10k_006505 | 9 | non-template short-circuit lines: 'Note: <op> is the QUERY operator ... Proceed to answer directly.' + Summary prepends 'Query operator <op> = ..., no need to solve the other unknowns;' (template Summary is bare 'Confirm reading order = ...') |
| crypt10k_006507 | 9 | non-template short-circuit lines: 'Note: <op> is the QUERY operator ... Proceed to answer directly.' + Summary prepends 'Query operator <op> = ..., no need to solve the other unknowns;' (template Summary is bare 'Confirm reading order = ...') |
| crypt10k_006509 | 8 | non-template short-circuit lines: 'Note: <op> is the QUERY operator ... Proceed to answer directly.' + Summary prepends 'Query operator <op> = ..., no need to solve the other unknowns;' (template Summary is bare 'Confirm reading order = ...'); boxed answer contains a literal brace ('[{{<') -> first brace closes the box early (render ambiguity; not a generator rule violation) |
| crypt10k_006520 | 9 | non-template short-circuit lines: 'Note: <op> is the QUERY operator ... Proceed to answer directly.' + Summary prepends 'Query operator <op> = ..., no need to solve the other unknowns;' (template Summary is bare 'Confirm reading order = ...') |
| crypt10k_006532 | 9 | non-template short-circuit lines: 'Note: <op> is the QUERY operator ... Proceed to answer directly.' + Summary prepends 'Query operator <op> = ..., no need to solve the other unknowns;' (template Summary is bare 'Confirm reading order = ...') |
| crypt10k_006540 | 9 | non-template short-circuit lines: 'Note: <op> is the QUERY operator ... Proceed to answer directly.' + Summary prepends 'Query operator <op> = ..., no need to solve the other unknowns;' (template Summary is bare 'Confirm reading order = ...') |
| crypt10k_006544 | 9 | non-template short-circuit lines: 'Note: <op> is the QUERY operator ... Proceed to answer directly.' + Summary prepends 'Query operator <op> = ..., no need to solve the other unknowns;' (template Summary is bare 'Confirm reading order = ...') |
| crypt10k_006555 | 9 | non-template short-circuit lines: 'Note: <op> is the QUERY operator ... Proceed to answer directly.' + Summary prepends 'Query operator <op> = ..., no need to solve the other unknowns;' (template Summary is bare 'Confirm reading order = ...') |
| crypt10k_006564 | 9 | non-template short-circuit lines: 'Note: <op> is the QUERY operator ... Proceed to answer directly.' + Summary prepends 'Query operator <op> = ..., no need to solve the other unknowns;' (template Summary is bare 'Confirm reading order = ...') |
| crypt10k_006611 | 9 | non-template short-circuit lines: 'Note: <op> is the QUERY operator ... Proceed to answer directly.' + Summary prepends 'Query operator <op> = ..., no need to solve the other unknowns;' (template Summary is bare 'Confirm reading order = ...') |
| crypt10k_006641 | 9 | non-template short-circuit lines: 'Note: <op> is the QUERY operator ... Proceed to answer directly.' + Summary prepends 'Query operator <op> = ..., no need to solve the other unknowns;' (template Summary is bare 'Confirm reading order = ...') |
| crypt10k_006647 | 9 | non-template short-circuit lines: 'Note: <op> is the QUERY operator ... Proceed to answer directly.' + Summary prepends 'Query operator <op> = ..., no need to solve the other unknowns;' (template Summary is bare 'Confirm reading order = ...') |
| crypt10k_006649 | 9 | non-template short-circuit lines: 'Note: <op> is the QUERY operator ... Proceed to answer directly.' + Summary prepends 'Query operator <op> = ..., no need to solve the other unknowns;' (template Summary is bare 'Confirm reading order = ...') |
| crypt10k_006663 | 9 | non-template short-circuit lines: 'Note: <op> is the QUERY operator ... Proceed to answer directly.' + Summary prepends 'Query operator <op> = ..., no need to solve the other unknowns;' (template Summary is bare 'Confirm reading order = ...') |
| crypt10k_006665 | 9 | non-template short-circuit lines: 'Note: <op> is the QUERY operator ... Proceed to answer directly.' + Summary prepends 'Query operator <op> = ..., no need to solve the other unknowns;' (template Summary is bare 'Confirm reading order = ...') |
| crypt10k_006680 | 8 | non-template short-circuit lines: 'Note: <op> is the QUERY operator ... Proceed to answer directly.' + Summary prepends 'Query operator <op> = ..., no need to solve the other unknowns;' (template Summary is bare 'Confirm reading order = ...'); boxed answer contains a literal brace ('}//^') -> first brace closes the box early (render ambiguity; not a generator rule violation) |
| crypt10k_006697 | 8 | non-template short-circuit lines: 'Note: <op> is the QUERY operator ... Proceed to answer directly.' + Summary prepends 'Query operator <op> = ..., no need to solve the other unknowns;' (template Summary is bare 'Confirm reading order = ...'); boxed answer contains a literal brace ('{{){') -> first brace closes the box early (render ambiguity; not a generator rule violation) |
| crypt10k_006726 | 9 | non-template short-circuit lines: 'Note: <op> is the QUERY operator ... Proceed to answer directly.' + Summary prepends 'Query operator <op> = ..., no need to solve the other unknowns;' (template Summary is bare 'Confirm reading order = ...') |
| crypt10k_006766 | 8 | non-template short-circuit lines: 'Note: <op> is the QUERY operator ... Proceed to answer directly.' + Summary prepends 'Query operator <op> = ..., no need to solve the other unknowns;' (template Summary is bare 'Confirm reading order = ...'); boxed answer contains a literal brace (':}\%') -> first brace closes the box early (render ambiguity; not a generator rule violation) |
| crypt10k_006848 | 8 | non-template short-circuit lines: 'Note: <op> is the QUERY operator ... Proceed to answer directly.' + Summary prepends 'Query operator <op> = ..., no need to solve the other unknowns;' (template Summary is bare 'Confirm reading order = ...'); boxed answer contains a literal brace ('{}^%') -> first brace closes the box early (render ambiguity; not a generator rule violation) |
| crypt10k_006860 | 9 | non-template short-circuit lines: 'Note: <op> is the QUERY operator ... Proceed to answer directly.' + Summary prepends 'Query operator <op> = ..., no need to solve the other unknowns;' (template Summary is bare 'Confirm reading order = ...') |
| crypt10k_006861 | 9 | non-template short-circuit lines: 'Note: <op> is the QUERY operator ... Proceed to answer directly.' + Summary prepends 'Query operator <op> = ..., no need to solve the other unknowns;' (template Summary is bare 'Confirm reading order = ...') |
| crypt10k_006877 | 8 | non-template short-circuit lines: 'Note: <op> is the QUERY operator ... Proceed to answer directly.' + Summary prepends 'Query operator <op> = ..., no need to solve the other unknowns;' (template Summary is bare 'Confirm reading order = ...'); boxed answer contains a literal brace ('?}[}') -> first brace closes the box early (render ambiguity; not a generator rule violation) |
| crypt10k_006882 | 8 | non-template short-circuit lines: 'Note: <op> is the QUERY operator ... Proceed to answer directly.' + Summary prepends 'Query operator <op> = ..., no need to solve the other unknowns;' (template Summary is bare 'Confirm reading order = ...'); boxed answer contains a literal brace ('^""}') -> first brace closes the box early (render ambiguity; not a generator rule violation) |
| crypt10k_006895 | 9 | non-template short-circuit lines: 'Note: <op> is the QUERY operator ... Proceed to answer directly.' + Summary prepends 'Query operator <op> = ..., no need to solve the other unknowns;' (template Summary is bare 'Confirm reading order = ...') |
| crypt10k_006907 | 9 | non-template short-circuit lines: 'Note: <op> is the QUERY operator ... Proceed to answer directly.' + Summary prepends 'Query operator <op> = ..., no need to solve the other unknowns;' (template Summary is bare 'Confirm reading order = ...') |
| crypt10k_006911 | 8 | non-template short-circuit lines: 'Note: <op> is the QUERY operator ... Proceed to answer directly.' + Summary prepends 'Query operator <op> = ..., no need to solve the other unknowns;' (template Summary is bare 'Confirm reading order = ...'); boxed answer contains a literal brace ('#@@}') -> first brace closes the box early (render ambiguity; not a generator rule violation) |
| crypt10k_006937 | 8 | non-template short-circuit lines: 'Note: <op> is the QUERY operator ... Proceed to answer directly.' + Summary prepends 'Query operator <op> = ..., no need to solve the other unknowns;' (template Summary is bare 'Confirm reading order = ...'); boxed answer contains a literal brace ('>&{>') -> first brace closes the box early (render ambiguity; not a generator rule violation) |
| crypt10k_006954 | 9 | non-template short-circuit lines: 'Note: <op> is the QUERY operator ... Proceed to answer directly.' + Summary prepends 'Query operator <op> = ..., no need to solve the other unknowns;' (template Summary is bare 'Confirm reading order = ...') |
| crypt10k_006957 | 8 | non-template short-circuit lines: 'Note: <op> is the QUERY operator ... Proceed to answer directly.' + Summary prepends 'Query operator <op> = ..., no need to solve the other unknowns;' (template Summary is bare 'Confirm reading order = ...'); boxed answer contains a literal brace (''@{[') -> first brace closes the box early (render ambiguity; not a generator rule violation) |
| crypt10k_006978 | 9 | non-template short-circuit lines: 'Note: <op> is the QUERY operator ... Proceed to answer directly.' + Summary prepends 'Query operator <op> = ..., no need to solve the other unknowns;' (template Summary is bare 'Confirm reading order = ...') |
| crypt10k_006980 | 9 | non-template short-circuit lines: 'Note: <op> is the QUERY operator ... Proceed to answer directly.' + Summary prepends 'Query operator <op> = ..., no need to solve the other unknowns;' (template Summary is bare 'Confirm reading order = ...') |
