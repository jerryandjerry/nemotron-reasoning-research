# Line-by-line review of the generated CoT — id `b1b10e83`

Generated by the **newdata** model (greedy). 262 lines / 7,680 tokens / truncated / pred `10` / **WRONG** (gt `|"#$`).
Raw: `../../03_eval/2605231427_newdata_outproj_950val/val_eval_results.csv` (this id).

**Why `pred=10`:** the generation truncated with **no `\boxed{}`**, so the metric's last-resort fallback (`extract_final_answer` → "last number in the text") scraped the trailing `10` out of `(10B+A)(10A+B)` in the search. It's not an answer the model produced — just noise from the truncated loop.

## The puzzle and the correct one-step solve
```
"\+#| = "\#|        (operator +)
#!+#\ = #!#\        (operator +)
#"*#" = #){|        (operator *)
$\*)" = ${$#        (operator *)
)\+`# = )\`#        (operator +)
query: |"+#$        (operator +)      ground truth = |"#$
```
There are only **two** operators: `+` and `*`. `+` is **concatenation** — e.g. `"\ + #| -> "\#|` (operands glued). The query operator is `+`, so the answer is just `|"` ‖ `#$` = **`|"#$`**. No digit mapping, no arithmetic, no search. (Our training CoT does exactly this; moe got it right.)

## What the model actually did, line by line

| lines | what it does | verdict |
|---|---|---|
| 3–11 | **Legend** — maps 8 symbols (`" # $ ) \ | ` {`) | ❌ the puzzle has **9** digit symbols; **`!` (in EX2 `#!+#\`) is dropped entirely** |
| 12–16 | **Operators** — lists `+`→f, `*`→g, then **`)`→h and `$`→i** with made-up notes (*"note: ')' is used as an operator"*) | ❌ `)` and `$` are **digit symbols, not operators** — it **hallucinated two operators**; there are only 2 |
| 19–24 | **Convert to letter form** — equations mis-segment; **QUERY `|"+#$` becomes `FA AC`** | ❌ drops the operator and miswrites the digits (should be `FA f BC`) |
| 36 | `f: ... Output=AEBF. FWD matches -> f is concat_fwd.` | ✅ **correctly identifies `+` = concat_fwd** — at this point it has everything needed to answer |
| 41 | *"f is concat_fwd; **now search for g and h**"* | ❌ **the fatal turn** — instead of applying concat to the query (→ `|"#$`, done) it goes off to solve the *irrelevant* operators `g`=`*` and the hallucinated `h`=`)` (the query doesn't use them) |
| 43–91 | tries `g` = add/sub/mul/concat_rev (standard order), all "no value… backtrack"; then redoes the entire thing little-endian (61–91), all fail | ❌ fruitless, and over a corrupted puzzle |
| 93–262 | from *"I have to use heuristic search"* it repeats the **identical 3-line block ~56 times** until the 7,680 ceiling | ❌ **degenerate loop; never emits `\boxed{}`; no answer** |

The looped block (lines 94→262, verbatim, ×~56):
```
g: concat_fwd=BABA, concat_rev=ABAB. Output=BHDF. Neither matches, so g is not concat.
h: concat_fwd=DEEG, concat_rev=EGDE. Output=DEGD. Neither matches, so h is not concat.
i: not present in any example, so I have to guess what it means. Since it is $, ...
```

## Core findings
1. **Parsing wall** — it misreads the input before reasoning: drops `!`, invents `)` and `$` as operators (3–16). The invented operators come from its own training phrase ("operator unseen, guess…") misfiring on digit symbols.
2. **It HAD the answer and threw it away** — line 36 already solved the *query* operator (`+`=concat_fwd); line 41 abandoned it to chase operators the query never uses. This is a **planning/control failure, not arithmetic**.
3. **Then it loops to death** — an open search with no real state collapses into verbatim repetition until truncation. Output is empty of any boxed answer.

## Diff vs the training target
Our training CoT (73 lines): legend → identify `+`=concat_fwd from the examples → apply to the query → `|"#$`. Short, linear, correct. The model reproduced the *shape* (it even did the concat check at line 36) but not the *control* — it didn't stop and answer, it corrupted the input on the way in, and it never terminates. Same format, no execution.
