# Cryptarithm trainable-CoT audit — 2026-06-03

Method: the 20-agent third-party audit per `01_data/HOW_TO_AUDIT_A_NEMOTRON_CATEGORY.md` — 10 reasoning-cold
agents (question.txt + CoT only) + 10 format agents (cot_template.md + CoT), full disjoint-slice coverage of
the **275 trainable** CoTs (under 7680 tok AND GT-match). Un-coached prompts (Appendix A/B).

## Score distributions

| axis | n | mean | =10 | 7–9 | 4–6 | 0–3 |
|---|---|---|---|---|---|---|
| Reasoning (cold) | 272 | **9.29** | 146 | 122 | 4 | **0** |
| Format (template) | 271 | **9.02** | 111 | 132 | 28 | **0** |

Coverage ~99% (a few puzzles unscored by the slice agents). **No score below 4 on either axis; no 0–3.**

## Step 5 — extractor cross-check (mandatory): PASS
Ran the live metric (last non-empty `\boxed{}`, `rfind('}')`) over the whole corpus → **0 boxes extract
empty/NOT_FOUND**. So every `\boxed{}` is scorable; render-based flags are false-positives by construction.

## Reasoning findings (hand-verified)
- **4 sub-7s — query-only / unconstrained digit, silently picked (SOFT).** `arithmetic_196ff375` [5],
  `arithmetic_1c2e9814` [5], `arithmetic_6183bab7` [6], `arithmetic_65add53a` [6]. A digit symbol that the
  examples do not pin (appears only in the query, or appears only in an identity example that cancels it) is
  resolved by the DFS branch picking the first value, and the CoT declares "N of N resolved" without flagging
  the ambiguity. The boxed answer still matches gold (our first-DFS convention = the gold's), so it is not
  *wrong* — but it overclaims determination, unlike the unseen-**operator** case which honestly says "guess".
  Verified on `arithmetic_196ff375` (H∈{4,8,9}, takes H=4). **Optional fix:** flag a query-only/unconstrained
  digit as a stated guess, the same way unseen operators are flagged.
- **7–9 band = the ALLOWED soft pattern.** Overwhelmingly "unseen query operator → guess ~concat/by the
  distinct-operator rule" and "unseen result digit → first-of-remaining symbol by a stated tie-break." Per the
  rubric these are deterministic guesses by a *stated repeatable rule* — permitted, not bugs.
- One harmless blemish: `little_endian_ae4aef23` [7] keeps a spurious `lock f=a+b-2` inside a §1 branch it then
  abandons; the final §2 answer is correct and self-contained.
- **No backward reasoning, no internal contradictions, no derivation-vs-box mismatch found.**

## Format findings (hand-verified)
- **The sub-10s are TEMPLATE STALENESS, not CoT defects.** `cot_template.md` predates this session's wording
  (spacing, `×`/`in`/`rhs`, numeric vocabulary). The format auditors flag the generator's current wording as
  "non-template" — but every CoT matches the worked example and is internally consistent. 27 `pure_concat`
  puzzles [6] were all flagged for the same two items: the opening phrase and the concat-lock line
  (`a∥b matches -> so lock g = a∥b` vs the template's `concat_fwd = a∥b` form). House-style; clears if the
  template is refreshed to the current generator.
- **`mixed_concat_c922dbb4` [4] — FALSE POSITIVE (render).** Auditor read the `{`/`}` answer symbols as a
  broken box; extractor yields `>@`, gold is `>@`. Verified clean. Killed per Step 5.
- No structural defects (sections present & ordered in all puzzles); answer rendering correct.

## Bottom line
The trainable corpus is clean: **0 severe defects, 0 broken boxes, no backward/contradictory reasoning.** Two
optional follow-ups: (1) flag the 4 query-only-digit picks as guesses for honesty; (2) refresh
`cot_template.md` to the current wording so the format axis stops flagging house-style. Single-reviewer
coverage — a broad one-pass sweep, not a proof of zero defects.
