# bit_manipulation faithful corpus — audit report (2026-06-12)

Corpus: 260612_BitM, the 1,510 GT CoTs from `_gen_bitm_faithful.py` (huikang unsteered machinery +
honest scan tiers + Pattern consistency check). Method: HOW_TO_AUDIT_A_NEMOTRON_CATEGORY.md.

## Coverage
- Reasoning: 24 cold auditors, all 1,510 CoTs (question.txt + tree_cot.txt only; no generator/template/gold).
- Format: 12 auditors vs cot_template_bitm.md, all 1,510.
- Step-5 metric cross-check: 1,510/1,510 boxes extract to valid 8-bit answers.
- Deterministic full-corpus scans: 0 internal contradictions, 0 unsound traces (every Selected rule fits
  all examples; boxed = rules applied), 534/534 Pattern-check revised-column claims true.

## Scores
- Reasoning: mean 9.63 — 1059×10, 435×(7-9), 11×(4-6), 5×(0-3).
- Format:    mean 9.73 — 1293×10, 182×(7-9), 33×(4-6), 2×(0-3).

## Pattern consistency check — UNANIMOUSLY judged honest
Every reasoning auditor that saw a §7b block independently reconstructed the stated global rule, confirmed
it reproduces every example at all 8 positions and yields the boxed answer, and confirmed each revised rule
also fits every example (genuine ambiguity, not backward-from-gold). No dishonest/unsupported revision of
the rule itself was found. Soft note (scored 9): the "already match / revise to" phrasing slightly overstates
forcedness at genuinely-ambiguous positions — rhetorical, not a defect.

## Defects found and resolved
1. **Conjured revision (184 rows, format-flagged):** the revised rule (often a tier-family Ch/Maj) was not
   surfaced by the pending-scoped, hits-only scan above, so it read as asserted. FIXED: the revise line now
   shows the revised rule's column inline and that it equals the output column (self-supporting). Verified:
   534/534 column claims true.
2. **default-1 guess (10 rows, GT_GUESS):** residual rows where no rule fit and the trace falls to `default 1`
   on a mixed column — a guess, not a derivation. ACTION: dropped to 0x (excluded from training).
3. **all-8-revised edge (d9ce7d0d):** empty "Positions  already match" line. FIXED (line omitted when empty).
4. **Right-before-Left ordering (~3 rows, format):** huikang's native convention (longer side first); not a
   CoT defect — spec note only.

## Trainable result
1,500 honest GT (excl. 10 default-1 guesses); ~1,494 under the 7,680 cap. +~140 vs huikang's 1,354, with the
gains concentrated in the hard/unused slice (165 of the 248 golden-uncovered puzzles now solved).
